import 'package:flutter/material.dart';

class PresenceEmployeePage extends StatelessWidget {
  const PresenceEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Presensi Employee Page'),
    );
  }
}
