import 'package:flutter/material.dart';

class AccountEmployeePage extends StatelessWidget {
  const AccountEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Akun Employee Page'),
    );
  }
}
