import 'package:flutter/material.dart';

class TrackEmployeePage extends StatelessWidget {
  const TrackEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Track Employee Page'),
    );
  }
}
