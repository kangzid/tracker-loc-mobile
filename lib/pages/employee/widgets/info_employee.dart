import 'package:flutter/material.dart';

class InfoEmployeePage extends StatelessWidget {
  const InfoEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Info Employee Page'),
    );
  }
}
