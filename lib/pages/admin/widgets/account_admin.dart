import 'package:flutter/material.dart';

class AccountAdminPageContent extends StatelessWidget {
  const AccountAdminPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Account Admin Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
