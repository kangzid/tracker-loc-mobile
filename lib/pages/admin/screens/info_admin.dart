import 'package:flutter/material.dart';

class InfoAdminPageContent extends StatelessWidget {
  const InfoAdminPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Info Admin Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
