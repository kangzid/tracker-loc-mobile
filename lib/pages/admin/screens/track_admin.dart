import 'package:flutter/material.dart';

class TrackAdminPageContent extends StatelessWidget {
  const TrackAdminPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Track Admin Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
