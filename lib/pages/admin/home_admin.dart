import 'package:flutter/material.dart';

class HomeAdminPageContent extends StatelessWidget {
  const HomeAdminPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Beranda Admin Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
